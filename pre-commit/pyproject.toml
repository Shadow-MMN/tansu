[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tansu_pre_push"
version = "1.0.0"
dependencies = [
    "soroban",
]

[project.scripts]
tansu-pre-push = "tansu_pre_push:main"

[tool.hatch.build]
include = ["tansu_pre_push.py"]
